services:
  spring-security-api:
    image: spring-security-api-image
    container_name: spring-api-container
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      # Variables para que Spring conecte con el Redis container
      - SPRING_DATA_REDIS_HOST=${SPRING_DATA_REDIS_HOST}
      - SPRING_DATA_REDIS_PORT=${SPRING_DATA_REDIS_PORT}

      - JDBC_DRIVER=${JDBC_DRIVER}
      - JDBC_HOST=${JDBC_HOST}
      - JDBC_PORT=${JDBC_PORT}
      - JDBC_DATABASE=${JDBC_DATABASE}
      - JDBC_SCHEMA=${JDBC_SCHEMA}
      - JDBC_USERNAME=${JDBC_USERNAME}
      - JDBC_PASSWORD=${JDBC_PASSWORD}

      - JWT_SECRET=${JWT_SECRET}
      - JWT_EXPIRATION_MINUTES=${JWT_EXPIRATION_MINUTES}

      - APP_NAME=${APP_NAME}
    networks:
      - spring-network
    depends_on:
      redis:
        # Se asegura que Redis esté healthy antes de conectar la API
        condition: service_healthy
      postgres:
        condition: service_started

  redis:
    image: redis:7-alpine
    container_name: redis-cache
    ports:
      - "${SPRING_DATA_REDIS_PORT}:${SPRING_DATA_REDIS_PORT}"
    networks:
      - spring-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  postgres:
    image: postgres:latest
    container_name: postgres_database
    environment:
      POSTGRES_DB: ${JDBC_DATABASE}
      POSTGRES_USER: ${JDBC_USERNAME}
      POSTGRES_PASSWORD: ${JDBC_PASSWORD}
    ports:
      - "${JDBC_PORT}:${JDBC_PORT}"
    networks:
      - spring-network
    volumes:
      - postgres-volume:/var/lib/postgresql/data/security-db

networks:
  spring-network:
    driver: bridge

volumes:
  postgres-volume: